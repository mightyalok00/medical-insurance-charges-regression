# Contributing

Contributions should keep the project reproducible and portfolio-friendly.

Guidelines:
- keep preprocessing inside pipelines where appropriate
- avoid using the final test set for tuning
- document new metrics and assumptions
- keep generated outputs under `reports/` and `images/`
- update documentation when behavior changes
- avoid committing caches, temporary files, or machine-specific artifacts
