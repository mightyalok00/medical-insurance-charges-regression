# Streamlit Dashboard Guide

Run locally with:

```bash
streamlit run app.py
```

The dashboard includes:
- executive overview
- predictions from the trained regression pipelines
- model-performance comparison
- business insights and feature importance
- interactive emoji-based filters
- filtered-data exploration and downloads
- access to saved project reports

The app resolves project files relative to `app.py` for portability.
