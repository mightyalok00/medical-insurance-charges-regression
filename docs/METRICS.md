# Evaluation Metrics

The project compares all three regression models with the same core metrics:

- **MAE** — average absolute prediction error.
- **MSE** — average squared prediction error.
- **RMSE** — error magnitude in the same units as insurance charges.
- **R²** — proportion of target variance explained by the model.

Metrics are interpreted together rather than relying on a single score.
